require('./config');

const Schema = require('./schema_v1')

// O tipo Buffer é usado para salva arquivos e retorna-los daforma, de Buffer, que conhecemos no Node.js, porem o MOngodb converte para Binary

// const imageSchema = new Schema({
//     mime: String,
//     bin: Buffer
// })

